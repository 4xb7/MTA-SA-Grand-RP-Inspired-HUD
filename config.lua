Saif = Rolex or Store or {

    ServerName = 'Server #e10f28Name', -- Server name

    ID = {
        enable = true, -- true = enable, false = disable
        color = tocolor(225, 15, 40, 255), -- RGBA color
    },

    Bookmark = { -- Top-right server number badge
        enable = true, -- true = enable, false = disable
        servernum = 1, -- Server number
    },

    CP = { -- Bottom-left compass
        enable = true, -- true = enable, false = disable
        color = tocolor(225, 15, 40, 255), -- RGBA color
    },

    MoneyBankData = 'moneyBank', -- Element data for bank balance
    IDData = 'ID', -- Element data for player ID
    GreenZoneData = 'greenzone', -- Element data for green zone status
}
